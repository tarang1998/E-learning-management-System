import 'package:flutter/material.dart';

class RegisterCoursePage extends StatelessWidget {
  const RegisterCoursePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Text("Register Course View"));
  }
}